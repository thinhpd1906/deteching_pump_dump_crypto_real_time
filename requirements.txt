# Core data
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# ML / training
scikit-learn>=1.3
torch>=2.1
xgboost>=2.0       # optional, B6 baseline

# Utilities
pyyaml>=6.0
tqdm>=4.66
requests>=2.31

# Visualization
matplotlib>=3.8
scipy>=1.11        # KS test in Gate C

# Real-time pipeline (optional -- only needed for live demo)
kafka-python>=2.0.2
websocket-client>=1.6
pyspark>=3.5        # or use python_bars.py fallback
